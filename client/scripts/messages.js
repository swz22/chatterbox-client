//This is the Model in MVC
//use this to add new messages to API
//pull all the data into this messages object


var Messages = {

};

// $('.submit')on.('click', function() {
//   Messages.username = App.username;
//   Messages.text = document.getElementById('message').value;
//   Messages.createdAt = Date.now();
//   Messages.updatedAt = Date.now();

//   Parse.create(Messages);
// });

/*
On click run function that builds the message by assigning username, roomnam and text values to the mesages object

Parse.create(Message) to submit to server
*/



//This might be where we add messages to the server as new Message objects


